import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FB2E86",
    },
    background: {
      // default: "#00FF00",
    },
  },
});
export default theme;
